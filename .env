# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and acquire TLS certificates
DOMAIN=localhost
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Deployment method. Docker Compose reads this variable itself, so it decides
# whether the Cloudflare Tunnel connector starts:
#   cloudflare -> run cloudflared; the tunnel handles public traffic
#   (empty)    -> don't; use the Traefik proxy (see deployment.md), or nothing
#                 at all, which is what local development and CI want
COMPOSE_PROFILES=

# Cloudflare Tunnel token, from Zero Trust > Networks > Tunnels.
# Only needed when COMPOSE_PROFILES=cloudflare, and every stack needs its own.
CLOUDFLARE_TUNNEL_TOKEN=

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=http://localhost:5173
# In staging and production, set this env var to the frontend host, e.g.
# FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Full Stack FastAPI Project"
STACK_NAME=full-stack-fastapi-project

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173,http://localhost.tiangolo.com"
SECRET_KEY=changethis
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=changethis

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

# Ports for local development
PROJECT_NUMBER=0
PROXY_HTTP_PORT=80
PROXY_DASHBOARD_PORT=8090
DB_PORT=5432
ADMINER_PORT=8080
BACKEND_PORT=8000
BACKEND_TEST_PORT=8100
MAILCATCHER_WEB_PORT=1080
MAILCATCHER_SMTP_PORT=1025
FRONTEND_PORT=5173
FRONTEND_TEST_PORT=5273
PLAYWRIGHT_PORT=9323
VITE_API_URL=http://localhost:8100
MAILCATCHER_HOST=http://localhost:1080
FRONTEND_TEST_URL=http://localhost:5273
