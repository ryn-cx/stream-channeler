1. Press Export on the [watches page](/watches) to download your watches
2. Upload the file here

Each exported watch carries its own verified status, so the import option below
only applies to entries from a file exported before that was recorded.
