# TODO: Validate
"""What the TMDB plugin is called.

All that is left of the identifier strings. The plugin row itself survives the
canonical split, because it still owns the `File` response cache, so its key is
still worth naming in one place.
"""

TMDB_PLUGIN_KEY = "TMDB"
